spring:
  profiles:
    active: dev
---
spring:
  datasource:
    url: jdbc:h2::./ATM
    username: bear-robotics
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      dialect: org.hibernate.dialect.H2Dialect
      ddl-auto: none
    show-sql: true
    generate-ddl: true
    format-sql: true
  sql:
    init:
      mode: always
      schema-locations: classpath*:init-query/schema.sql
      data-locations: classpath*:init-query/data.sql
  config:
    activate:
      on-profile:
---
spring:
  datasource:
    url: jdbc:h2::./ATM
    username: bear-robotics
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      dialect: org.hibernate.dialect.H2Dialect
      ddl-auto: validate
    show-sql: true
    generate-ddl: true
    format-sql: true
  config:
    activate:
      on-profile: test



